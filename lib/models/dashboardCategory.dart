import 'package:flutter/material.dart';

class Category {
  String? title;
  String? subTitle;
  IconData? icon;
  Color? color;
  

  Category(
      {this.title,
      this.subTitle,
      this.icon,
      this.color,
      });
}
